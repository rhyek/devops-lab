{
  "name": "todos",
  "scripts": {
    "dev": "$(bin-up ts-node-dev) src/main.ts",
    "build": "$(bin-up webpack) --config ./webpack.config.ts"
  },
  "dependencies": {
    "bin-up": "~1.3.2"
  }
}
